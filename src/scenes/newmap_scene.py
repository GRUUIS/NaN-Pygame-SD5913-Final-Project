"""newmap_scene removed

This scene was removed from the project. The module is left as a small
stub so imports fail clearly if code still tries to use it.
"""

def run(*args, **kwargs):
    """Stub run() that indicates the scene was removed and returns.

    Calling code should prefer `src.scenes.map01_scene` instead. This stub
    prints a message and returns immediately.
    """
    try:
        print('newmap_scene: scene removed from project; please use map01_scene instead')
    except Exception:
        pass
    return
